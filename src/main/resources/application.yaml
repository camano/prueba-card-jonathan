spring:
  application:
    name: prueba

  datasource:
    url : jdbc:postgresql://localhost:5432/card_prueba
    username: postgres
    password: root
  jpa:
    database : POSTGRESQL
    show-sql : true
    hibernate :
      ddl-auto : update
